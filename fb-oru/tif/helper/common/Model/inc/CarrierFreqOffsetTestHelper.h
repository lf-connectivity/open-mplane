/*!
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 * \file      CarrierFreqOffsetTestHelper.h
 * \brief     Test helper for common
 *
 *
 * \details   Auto-generated by gentool. Contains test interface definitions. Please add your own
 * code between the %%--> and <--%% comment lines (any code outside these comments will be discarded
 * next time the generate tool is run).
 *
 */

#ifndef __CARRIERFREQOFFSETTESTHELPER__H_
#define __CARRIERFREQOFFSETTESTHELPER__H_


//=============================================================================================================
// INCLUDE
//=============================================================================================================
#include <string>
#include <vector>
#include <memory>
#include "IRtiMsgParser.h"
#include "DataVariant.h"

// Add any specific includes here %%-->
#include "ICarrier.h"

// <--%%

//=============================================================================================================
// CLASS
//=============================================================================================================

namespace Mplane {

class CarrierFreqOffsetTestHelper
// Add any extra inheritance %%-->

// <--%%
{
public:

	// Add any class settings %%-->

	// <--%%

	// singleton
	static std::shared_ptr<CarrierFreqOffsetTestHelper> getInstance() ;


	CarrierFreqOffsetTestHelper();

    virtual ~CarrierFreqOffsetTestHelper();

	/**
	 * Called with:
	 *   INT Index
	 *
	 * Returns:
	 *   ENUM(OK:ERROR) Status=OK
	 *   [STRING Info]
	 *   [STRING Message]
	 *   FLOAT TxFreqOffset (kHz)
	 *   FLOAT RxFreqOffset (kHz)
	 *
	 */
	void getFreqOffset( IRtiMsgParser& msgParser, DataVariantList *callList, DataVariantList *returnList );

	/**
	 * Called with:
	 *   INT Index
	 *   [FLOAT TxFreqOffset (kHz)]
	 *   [FLOAT RxFreqOffset (kHz)]
	 *
	 * Returns:
	 *   ENUM(OK:ERROR) Status=OK
	 *   [STRING Info]
	 *   [STRING Message]
	 *
	 */
	void setFreqOffset( IRtiMsgParser& msgParser, DataVariantList *callList, DataVariantList *returnList );






// Add any extra methods %%-->
private:
	static ICarrierSetter& getCarrierSetter(bool& ok, DataVariantList *callList, DataVariantList *returnList);
	static const ICarrier& getCarrier(bool& ok, DataVariantList *callList, DataVariantList *returnList);
	static bool checkState(bool ok, const std::string& param, DataVariantList *returnList) ;

// <--%%

};



// Add any Mplane namespace code here %%-->

// <--%%


} // Mplane

// Add any global namespace code here %%-->

// <--%%


#endif /* __CARRIERFREQOFFSETTESTHELPER__H_*/
