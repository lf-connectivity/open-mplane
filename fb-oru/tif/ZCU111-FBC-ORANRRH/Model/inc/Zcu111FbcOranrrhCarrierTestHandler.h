/*!
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 * \file      Zcu111FbcOranrrhCarrierTestHandler.h
 * \brief     Test methods for ZCU111-FBC-ORANRRH
 *
 *
 * \details   Auto-generated by gentool. Contains test interface definitions. Please add your own
 * code between the %%--> and <--%% comment lines (any code outside these comments will be discarded
 * next time the generate tool is run).
 *
 */

#ifndef __ZCU111FBCORANRRHCARRIERTESTHANDLER__H_
#define __ZCU111FBCORANRRHCARRIERTESTHANDLER__H_


//=============================================================================================================
// INCLUDE
//=============================================================================================================
#include <string>
#include <vector>
#include <memory>
#include "CarrierTestHelper.h"
#include "ITif.h"
#include "IRtiMsgParser.h"
#include "Nodes/EntryFunc.h"

// Add any specific includes here %%-->

// <--%%

//=============================================================================================================
// CLASS
//=============================================================================================================

namespace Mplane {

class Zcu111FbcOranrrhCarrierTestHandler: public virtual ITifEntry, public EntryFunc
{
public:

	Zcu111FbcOranrrhCarrierTestHandler( const std::string& pathname, const std::string& comment,
    		const std::string& callFields, const std::string& returnFields,
    		IRtiMsgParser& msgParser, IRtiSettings& settings);

    virtual ~Zcu111FbcOranrrhCarrierTestHandler();

    static void registerMethods(IRtiMsgParser& msgParser, IRtiSettings& settings) ;

	/**
	 * Special constructor - only to be used for calling the static registerMethods()
	 */
	Zcu111FbcOranrrhCarrierTestHandler();

	/**
	 * Called by ITif::inject() to get this object to add it's entries
	 * @param tif
	 * @return true if ok
	 */
	virtual bool registerEntry(ITif* tif) override ;

protected:

    virtual std::string operator()(const std::string& functionName) ;

	/**
	 * Called with:
	 *   INT Index
	 *
	 * Returns:
	 *   ENUM(OK:ERROR) Status=OK
	 *   [STRING Info]
	 *   [STRING Message]
	 *
	 */
	void del( DataVariantList *callList, DataVariantList *returnList );

	/**
	 * Called with:
	 *   INT Index
	 *
	 * Returns:
	 *   ENUM(OK:ERROR) Status=OK
	 *   [STRING Info]
	 *   [STRING Message]
	 *   INT Index
	 *   ENUM(${CARRIER_TYPES}) Type
	 *   ENUM(${CARRIER_STATES}) State
	 *   ENUM(${CARRIER_SIGNALS}) SigType
	 *   FLOAT TxPower (dBm)
	 *   FLOAT TxFreq (kHz)
	 *   FLOAT RxFreq (kHz)
	 *   INT GroupUp
	 *   INT GroupDown
	 *   FLOAT TxScaling (dBm)
	 *   FLOAT UplinkNoise (dBm)
	 *   BOOL(off:on) Loopback
	 *   INT[] TxContainer
	 *   INT[] RxContainer
	 *   INT[] TxFibre
	 *   INT[] RxFibre
	 *   INT UlDlConfig
	 *   INT SSConfig
	 *   ENUM(normal:extended) UlPrefixLen
	 *   ENUM(normal:extended) DlPrefixLen
	 *   ENUM(NONE:1kHz25:3kHz75:5kHz:7kHz25:15kHz:30kHz:60kHz:120kHz:240kHz) ulScs
	 *   ENUM(NONE:1kHz25:3kHz75:5kHz:7kHz25:15kHz:30kHz:60kHz:120kHz:240kHz) dlScs
	 *   ENUM(NONE:256:512:1024:1536:2048:4096) ulFFT
	 *   ENUM(NONE:256:512:1024:1536:2048:4096) dlFFT
	 *   INT[] ulEAXCID
	 *   INT[] dlEAXCID
	 *   STRING ulMAC
	 *   ENUM(LTE:5GNR) mode
	 *   INT ulCp0Size
	 *   INT dlCp0Size
	 *   INT ulCp1Size
	 *   INT dlCp1Size
	 *
	 */
	void get( DataVariantList *callList, DataVariantList *returnList );

	/**
	 * Called with:
	 *   INT Index
	 *
	 * Returns:
	 *   ENUM(OK:ERROR) Status=OK
	 *   [STRING Info]
	 *   [STRING Message]
	 *   FLOAT[] Rssi (dBm)
	 *
	 */
	void getRssi( DataVariantList *callList, DataVariantList *returnList );

	/**
	 * Called with:
	 *   INT Index
	 *   [ENUM(${CARRIER_TYPES}) Type]
	 *   [ENUM(${CARRIER_STATES}) State]
	 *   [ENUM(${CARRIER_SIGNALS}) SigType]
	 *   [FLOAT TxPower (dBm)]
	 *   [FLOAT TxFreq (kHz)]
	 *   [FLOAT RxFreq (kHz)]
	 *   [INT GroupUp]
	 *   [INT GroupDown]
	 *   [FLOAT TxScaling (dBm)]
	 *   [INT[] TxContainer]
	 *   [INT[] RxContainer]
	 *   [INT[] TxFibre]
	 *   [INT[] RxFibre]
	 *   [FLOAT UplinkNoise (dBm)]
	 *   [BOOL(off:on) Loopback]
	 *   [INT uldlConfig]
	 *   [INT ssConfig]
	 *   [ENUM(normal:extended) ulPrefixLen]
	 *   [ENUM(normal:extended) dlPrefixLen]
	 *   [ENUM(NONE:1.25KHZ:3.75KHZ:5KHZ:7.25KHZ:15KHZ:30KHZ:60KHZ:120KHZ:240KHZ) ulScs]
	 *   [ENUM(NONE:1.25KHZ:3.75KHZ:5KHZ:7.25KHZ:15KHZ:30KHZ:60KHZ:120KHZ:240KHZ) dlScs]
	 *   [ENUM(NONE:256:512:1024:1536:2048:4096) ulFFT]
	 *   [ENUM(NONE:256:512:1024:1536:2048:4096) dlFFT]
	 *   [INT[] ulEAXCID]
	 *   [INT[] dlEAXCID]
	 *   [STRING ulMAC]
	 *   [ENUM(LTE:5GNR) mode]
	 *   [INT ulCp0Size]
	 *   [INT dlCp0Size]
	 *   [INT ulCp1Size]
	 *   [INT dlCp1Size]
	 *
	 * Returns:
	 *   ENUM(OK:ERROR) Status=OK
	 *   [STRING Info]
	 *   [STRING Message]
	 *
	 */
	void set( DataVariantList *callList, DataVariantList *returnList );

	/**
	 * Called with:
	 *   INT Index
	 *
	 * Returns:
	 *   ENUM(OK:ERROR) Status=OK
	 *   [STRING Info]
	 *   [STRING Message]
	 *
	 */
	void show( DataVariantList *callList, DataVariantList *returnList );

	/**
	 * Called with:
	 *
	 *
	 * Returns:
	 *   ENUM(OK:ERROR) Status=OK
	 *   [STRING Info]
	 *   [STRING Message]
	 *
	 */
	void showAll( DataVariantList *callList, DataVariantList *returnList );



	/**
	 * Getter method for read-only variable NumCarriers
	 *
	 * @param pathname Variable path name used as the variable name in the returnList
	 * @param returnList DataVariantList use to return variable value
	 *
	 */
	static void NumCarriersGet(  const std::string& pathname, DataVariantList *returnList ) ;

	/**
	 * Getter method for read-only variable RxPaths
	 *
	 * @param pathname Variable path name used as the variable name in the returnList
	 * @param returnList DataVariantList use to return variable value
	 *
	 */
	static void RxPathsGet(  const std::string& pathname, DataVariantList *returnList ) ;

	/**
	 * Getter method for read-only variable TxPaths
	 *
	 * @param pathname Variable path name used as the variable name in the returnList
	 * @param returnList DataVariantList use to return variable value
	 *
	 */
	static void TxPathsGet(  const std::string& pathname, DataVariantList *returnList ) ;




// Add any extra methods %%-->

// <--%%

private:
	std::shared_ptr<CarrierTestHelper> mHelper ;
};



} // Mplane

#endif /* __ZCU111FBCORANRRHCARRIERTESTHANDLER__H_*/
